# Delivery photo production provisioning status and enablement gate

Phase 2A implemented attempt-specific immutable objects without changing production resources. Phase 2B Batches A/B/C subsequently provisioned and verified the backend resources below from source HEAD `a76871802374af8a3565f22c88c8b4166f8256c1` (project `onnuriway`, number `347044588399`). That backend-only checkpoint originally left the production frontend flag OFF. Phase 3A/3B/3C and the Galaxy field-card refinement are now complete at application HEAD `7ad3ab89c7e020fbfabcd7d144f8d6fec4ef511d`, and the delivery-photo frontend is enabled in production Hosting.

## Verified production infrastructure (2026-09-23)

- The dedicated Standard bucket is `prod-delivery-photos-an3-68e5c4d72a90` in `asia-northeast3`: Uniform bucket-level access ON, Public Access Prevention enforced, Soft Delete OFF, Object Versioning OFF, Autoclass OFF, no retention policy or default hold, and one bucket-wide Lifecycle Delete rule at age 8 days. Its object count was 0 after Batch C. The existing business bucket `onnuriway.firebasestorage.app` was not changed.
- The active runtime identity is `delivery-photo-runtime@onnuriway.iam.gserviceaccount.com`. Its only project role is `roles/datastore.user`. On the dedicated bucket, `projects/onnuriway/roles/deliveryPhotoObjectRuntime` contains exactly `storage.objects.create`, `storage.objects.get`, and `storage.objects.delete`: create/get/delete GRANTED, list/update DENIED. On the existing business bucket, create/get/delete/list/update are all DENIED. The M2 legacy ACL baseline found no access path for this new principal; it does not rely on project-level Storage Admin/Editor.
- `functions/.env.onnuriway` is gitignored and has the configured non-secret `DELIVERY_PHOTO_BUCKET` and `DELIVERY_PHOTO_SERVICE_ACCOUNT` keys. The deployed Functions' corresponding environment values matched the expected bucket and SA without exposing the rest of the env file.
- Exactly nine delivery-photo Functions were targeted for deployment: `getDeliveryPhotoRoute`, `saveDeliveryPhotoRoute`, `getDeliveryPhotoDay`, `saveDeliveryPhotoDay`, `createDeliveryPhoto`, `listDeliveryPhotos`, `getDeliveryPhoto`, `deleteDeliveryPhoto`, and `expireDeliveryPhotos`. All are ACTIVE, Gen2, `asia-northeast3`, Node.js 22, and use the dedicated runtime SA. `createDeliveryPhoto` has 1GiB memory, concurrency 1, maxInstances 4, timeout 120 seconds; `expireDeliveryPhotos` has maxInstances 1 and timeout 120 seconds. The previous 62 production Functions retained their names, states, runtimes, service accounts, and update times.
- The Firebase-managed job `firebase-schedule-expireDeliveryPhotos-asia-northeast3` is ENABLED, runs `every 60 minutes` in `Asia/Seoul`, and uses the dedicated SA as OIDC identity. That SA has `roles/run.invoker` on the exact scheduled Cloud Run service, not a project-wide invoker grant. Batch C did not manually invoke the job; no attempt had been recorded at verification time.
- Two `deliveryPhotos` COLLECTION composite indexes are READY: `deliveryDateKey ASC, createdAt DESC` (`CICAgJjmiJEK`) and `customerId ASC, createdAt DESC` (`CICAgNi47oMK`). The previous five indexes are unchanged. Neither `deliveryPhotos` nor `deliveryPhotoDays` has TTL enabled.
- Batch C's unauthenticated `getDeliveryPhotoRoute` Callable smoke returned HTTP 401 with `application/json` and `error.status=UNAUTHENTICATED`. No authenticated production delivery-photo Callable, production photo/document write, or Hosting/frontend deploy was performed. The Firestore business collection document count was not queried. No rollback was needed.

## Production feature enablement boundary

Items 1–6 guided backend provisioning and were verified through the actual bucket/IAM configuration or the cited source tests, as applicable. Item 7 was the original frontend enablement gate and was satisfied on 2026-09-24. Keep this checklist for future delivery-photo releases or infrastructure changes:

1. `DELIVERY_PHOTO_BUCKET` names the dedicated delivery-photo bucket in the intended `asia-northeast3` region. Verify the actual bucket location; the default application bucket is not a fallback.
2. Every service-generated upload path is under the managed `delivery-photos/` prefix and has the shape `delivery-photos/{deliveryDateKey}/{photoId}/attempts/{uploadAttemptToken}/{evidence|thumbnail}.webp`. Unit tests cover the path predicate and reject an upload outside its own attempt path.
3. The bucket does not allow public access. Verify that Public Access Prevention is **enforced** and confirm the Uniform bucket-level access setting on the actual bucket.
4. An Object Lifecycle **Delete** rule covers every managed attempt path under `delivery-photos/` at age **8 days**. Inspect the deployed rule's action, age, and prefix applicability; a planned rule is insufficient.
5. Soft Delete is **OFF** and Object Versioning is **OFF**. Verify that no retention policy, default event-based hold, or other hold prevents lifecycle or application deletion.
6. Verify effective IAM on the dedicated bucket: the delivery-photo runtime identity has only the object permissions it needs on that bucket, has no unnecessary access to existing business buckets, and does not depend on broad project-level Storage Admin permissions.
7. Before initial enablement, keep the production delivery-photo feature disabled until the bucket and effective IAM checks above remain true and the Phase 3 frontend/mobile field UX, application, Rules, emulator, and build gates are complete. This gate is now satisfied: the current production Hosting build has `NEXT_PUBLIC_ENABLE_DELIVERY_PHOTOS=true`, and the delivery-photo workspace, camera capture, album upload, history, and viewer are exposed to authorized users.

## Dedicated runtime identity

- The dedicated service account now exists: ID `delivery-photo-runtime`, email `delivery-photo-runtime@onnuriway.iam.gserviceaccount.com`.
- Its full email is supplied through the server-only `DELIVERY_PHOTO_SERVICE_ACCOUNT` project-specific Functions environment configuration. The nine delivery-photo exports alone use it; the existing Functions' runtime identities and global options were not changed.
- A non-emulator Functions discovery/runtime import fails if the setting is absent, malformed, points outside the target project, or names a recognized default identity. The local Functions emulator omits the service-account option and does not require a production account. The selector/discovery and actual deployed identities were checked around Batch C; repeat those checks before any future deployment.
- The pre-deployment identity/IAM blocker was cleared before Batch C. Continue to verify both bucket and project policies, including inherited grants, before any future identity or permission change; a bucket-local policy alone is insufficient.

## Runtime permissions and remaining live checks

- **Required by current code:** On the dedicated bucket, object create, get (including metadata and download), and delete. The Storage adapter does not list objects or update existing objects. In Firestore, the server SDK reads documents and queries and performs transactional create/update/delete for route, day, photo, request receipt/lock, rate-limit, customer/employee authorization, and audit records.
- **Provisioned:** `roles/datastore.user` is the runtime SA's sole project role. The dedicated bucket uses the narrower custom role above rather than `roles/storage.objectUser`.
- **Not required by the delivery-photo runtime code:** Storage bucket administration, object list/update, Firebase Auth user administration, Scheduler administration, or project-level Storage Admin/Editor. Deployment operator permissions and Scheduler invocation identity are separate from the Function runtime identity.
- **Current live evidence and remaining checks:** after enablement, the authorized user verified camera upload, album upload, additional and sequential photos, server-confirmed completion movement, history thumbnails, evidence viewing, previous/next, and Android Back on a Galaxy S20+. These observations cover the user-visible authenticated create/list/get paths but do not replace infrastructure-level fault injection. A production network failure/retry scenario, actual scheduled invocation, Cloud Logging review, and deliberate GCS generation-precondition failure are not recorded as complete; do not create production data merely to satisfy those checks without separate approval.

Application cleanup deletes normal expired photos and known orphan targets promptly. The two-hour `recoveryUntil` is an active rescan period, not proof that a pending upload cannot finish later. Immutable attempt paths keep such an upload separate from the winning photo. The provisioned bucket-wide age-8-day lifecycle is the final bound for an unknown late orphan after the worker and receipt recovery have ended. Unit tests prove path containment and data isolation; they do not simulate actual GCS lifecycle execution.

## Phase 3 production completion (2026-09-24)

- Phase 3A provides today's remaining stops, completion projection from photo metadata count, default route and today override editing, customer add/remove/reorder/search, recent-20 reuse, Memory-only state, and revision conflict handling.
- Phase 3B provides camera and album input, max-long-edge 2560 fresh WebP processing without crop/upscale, EXIF/GPS removal, a Memory-only upload coordinator, one active job per customer, preparation concurrency 1, at most two Callable relays, duplicate-tap protection, stable request ID/payload retry, and completion only after server-confirmed `createDeliveryPhoto` metadata. It has no persistent/offline upload queue and no direct Client Storage write.
- Phase 3C provides customer-scoped history, metadata-first list rendering, viewport thumbnail lazy load, selected evidence lazy load, registrant/time metadata, previous/next, keyboard, Escape, Android Back, pointer swipe, and Memory-only Object URL cleanup without persistent photo caching.
- The field-card refinement displays existing `Customer.accessPassword` only for `accessPasswordState === "registered"`, performs no additional customer request, removes the separate completed-row `사진 보기` button, and keeps the left History action, camera, and album as sibling controls.
- Galaxy S20+ production checks passed for camera/album upload, additional and sequential photos, history, thumbnail, evidence viewer, previous/next, and Back/history retention. The final card UX also passed on the device.
- Production Hosting release `1790211742342000`, version `e603258b088093f0`, went live at `2026-09-24 10:02:22.342 KST`. The Service Worker is `/sw.js` with SHA-256 `20520cc6aaa7d8b1ca5ee67522e41b9dc4f691429e8b940b7c154ecb52d5291d`; `onnuriway.com` and `onnuriway.web.app` matched the candidate worker and all 82 precache assets. The previous rollback target is version `d87b75d47936e2c3`.
- The Hosting release changed no Functions, Firestore, Storage, IAM, Scheduler, Rules, indexes, or extensions. The nine delivery-photo Functions, dedicated runtime identity and bucket, custom IAM, two READY indexes, hourly expiration Scheduler, age-8-day lifecycle, and business-bucket isolation remain as provisioned in Phase 2B.
- Current bundle measurements are: workspace JS 10,239B gzip against a 10,240B budget (1B headroom), history JS 3,459B gzip, viewer JS 2,811B gzip, workspace CSS 6,066B raw / 1,584B gzip, and initial JS 141,040B gzip. Treat the workspace as at its ceiling; prefer event-loaded/lazy splits over adding code or relaxing the budget.
- Final gates passed: app/Functions typecheck, backend contract/Functions 54 tests, frontend 50 tests, Demo Emulator Chromium 6/6, lint, production build, PWA/performance/Hosting gates, `git diff --check`, 320/360/200% overflow checks, accessibility, and production Galaxy validation.
- Delete UI, Web Share, and GPS nearby suggestion remain separate candidates, not completed work. Network failure/retry is implemented and Emulator-covered but is not recorded as production fault-injection complete.
