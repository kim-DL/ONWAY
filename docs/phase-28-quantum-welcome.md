# Quantum 웰컴 장식 — 2026-09-06

## 요청과 범위

- 사용자가 제공한 21st.dev `quantum-cloud-loader` 코드의 네 입자 궤도를 바탕으로
  기존 GIF 캐릭터를 코드 기반 장식으로 교체한다.
- 웰컴 카드 끝으로 밀지 않고, 큰 제목 첫 문구 바로 옆에 묶어 배치한다.
- 제목 둘째 줄은 전체 폭을 사용한다. 이름은 실제 사용 조건인 7자 이내를
  우선 기준으로 삼고 확대 글자 및 좁은 화면에서도 기능이 깨지지 않게 한다.
- 납품·영업 홈과 활동의 기존 문구·제목 ID·로고·모드 버튼 및 업무 기능 유지.

## 참고 코드의 적용

- 기존 `src/components/ui` 구조·TypeScript·CSS 구성에 맞춰 독립 컴포넌트와
  CSS Module로 작성. 외부 라이브러리·이미지·폰트·새 페이지를 추가하지 않았다.
- 첨부 데모의 최소 200px 높이와 전체 화면 래퍼는 넣지 않는다.
- 블루·코랄·샴페인·세이지의 고정 색상 코어, 작은 광택과 절제된 그림자로 조정.
- 네 입자의 크기와 속도를 다르게 유지. 4.2/6.8/3.6/8.6초이며 마지막 입자는
  왕복 반복한다. 나머지 궤도는 시작/끝 위치와 불투명도가 같아 경계 점프가 없다.
- 프레임별 React 업데이트나 타이머 없이 transform·opacity만 움직인다.

## 안전성·접근성·업데이트

- 장식이며 실제 로딩 상태가 아니다. role=status/aria-busy/포커스를 추가하지 않는다.
- 최초 서버 렌더에서는 정지. 저장된 애니메이션 설정 및 OS 동작 감소를 확인한 뒤 재생.
- 설정 정지, 탭 비활성, 화면 밖에서는 CSS 재생을 멈추고 복귀 시 이어서 반복.
- 정지 시 입자가 한 점으로 합쳐지지 않고 현재 위치를 유지한다.
- 기존 GIF를 앱 렌더링·사전 캐시·허용 목록에서 제외해 해당 파일 요청을 없앤다.
  사용자 원본과 이전 자산 파일은 복구 가능하도록 보존한다.
- PWA 캐시 phase28. 설치된 앱의 업데이트 흐름은 유지한다.
- Firebase 서버·인증·보안 규칙·학교 및 방문 데이터는 변경하지 않는다.

## 검증

- 단위 검사 430개 통과, 기존 조건부 3개 제외.
- 독립 브라우저 검사 26개 통과. 납품·영업·팀·활동의 360/390/430/768/1280px
  배치와 320px·200% 글자 확대를 검사했다. 궤도 전체 9개 시점에서 제목 겹침 없음.
- 실제 CSS 동작·무한 반복·정지·OS 동작 감소 전환·화면 이탈·설정 유지·접근성 검사 통과.
- Firebase 에뮬레이터 인증 통합 시나리오 13개 통과. 실제 앱의 홈·활동 배치,
  모드 전환·애니메이션 설정과 방문 동선·학교 추가/해제 회귀 포함.
- 실제 앱 390px 홈·활동 캡처를 육안 확인. 제목 둘째 줄 폭과 기존 헤더 유지.
- 전체 ESLint, 앱·Functions TypeScript, 운영 환경 Next.js 빌드, PWA 빌드 검사 통과.
- 운영 설정의 로컬 빌드를 브라우저로 열어 PIN 입력 화면을 확인했다.
  로그인·데이터 변경 검사는 에뮬레이터에서만 수행했다.
- 기존 GIF의 앱 요청과 서비스 워커 사전 캐시 참조가 없는 것을 확인했다.

## 운영 반영

- 반영 전 운영: `dpl_ZvywRMFba6NfGNGB8PMdw8BsvTLj` (phase27)
  `https://onnuriway-9rpg3nuvb-daeins-projects.vercel.app`
- 코드 커밋: `938e1a2`.
- 배포: `dpl_GDCSKEqu9DtAoLjtdh9MFFnXDM62`, Production / Ready.
- 생성: 2026-09-06 02:02:38 KST. 운영 별칭: `https://onnuriway.vercel.app`.
- 불변 URL: `https://onnuriway-je2kpogao-daeins-projects.vercel.app`.
- 운영 브라우저의 phase27 서비스 워커에서 업데이트 안내가 나타나는 것을 확인하고
  실제 ‘업데이트’ 버튼으로 전환했다. 결과 캐시는 `app-shell-phase28`만 남았고
  기존 `bloub` 자산 캐시는 0개였다. 익명 PIN 화면 정상, 브라우저 오류 없음.
- 배포 후 조회한 해당 배포의 최근 10분 오류 로그 없음. 운영 직원 로그인이나
  업무 데이터 변경은 수행하지 않았으며 로그인 이후 검증은 위 에뮬레이터 결과다.
- 필요 시 위 phase27 배포로 되돌릴 수 있다. 서버·데이터 마이그레이션 없음.
