# Server-only NEIS import configuration. Never prefix these values with NEXT_PUBLIC_.
TARGET_EDUCATION_OFFICE_CODE=G10
NEIS_API_KEY=
ALLOW_LIVE_NEIS_IMPORT=false

# Phase 13 incremental synchronization requires an additional explicit gate.
ALLOW_LIVE_NEIS_SYNC=false

# Server-only Kakao Local REST configuration.
KAKAO_REST_API_KEY=
ALLOW_LIVE_KAKAO_MATCH=false

# Completed CSV exports remain downloadable for this many hours (1-168).
CSV_EXPORT_TTL_HOURS=24
