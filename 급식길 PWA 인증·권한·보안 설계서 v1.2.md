# 급식길 PWA 인증·권한·보안 설계서 v1.3

**문서 버전:** 1.3  
**대상:** 급식길 PWA MVP  
**주요 개정:** 휴대폰 파생 PIN 제거, 서버 무작위 PIN 발급, 관리자 Google 허용목록 인증 확정  
**관련 문서:** MVP 기획서 / 데이터베이스 상세 설계서 / 디자인 시스템 / 화면·UX 상세 명세서

---

# 1. 인증 설계 목표

급식길은 소수의 사내 직원이 현장에서 반복적으로 사용하는 업무용 PWA다.

따라서 일반적인 웹서비스처럼 이메일, 아이디, 복잡한 비밀번호를 매번 입력하는 방식보다 다음 원칙을 우선한다.

> **기억하기 쉬운 개인 PIN 하나로 최초 인증하고, 이후에는 사용자가 직접 로그아웃하지 않는 한 로그인 상태를 유지한다.**

인증 과정은 최대한 단순하게 만들되 다음 보안 장치를 서버에서 적용한다.

- PIN 평문 저장 금지
- PIN 중복 금지
- 로그인 시도 제한
- 비정상 로그인 잠금
- App Check
- 관리자 강제 세션 종료
- 직원 비활성화
- 권한 변경 시 세션 갱신
- Security Rules에 의한 데이터 접근 차단

---

# 2. 일반 직원 로그인 방식

일반 직원은 **개인 PIN 하나만 입력**한다.

사용하지 않는 로그인 정보:

- 직원코드
- 사용자명
- 이메일
- 별도 비밀번호

로그인 화면의 기본 구조는 다음과 같다.

```text
급식길

대전 학교 급식실 현장정보


개인 PIN

○ ○ ○ ○ ○ ○


[ 시작하기 ]
```

PIN은 다음 두 역할을 동시에 수행한다.

```text
직원 식별
+
사용자 인증
```

---

# 3. PIN 기본 규격

직원 PIN은 다음 규격을 사용한다.

```text
형식: 숫자
길이: 6자리
중복: 허용하지 않음
```

앞자리가 `0`인 경우도 그대로 6자리로 취급한다.

예:

```text
001234
258714
482915
```

---

# 4. PIN 부여 방식

PIN은 관리자 화면 또는 서버의 암호학적으로 안전한 난수 생성기로 발급한다.

## 4.1 기본 방식 — 서버 무작위 발급

```text
6자리 숫자 무작위 생성
↓
시스템 내 중복 확인
↓
금지 패턴 확인
↓
직원에게 안전한 채널로 전달
```

직원 코드, 별칭, 휴대폰 번호, 생년월일 등 개인·업무정보에서 PIN을 파생하지 않는다.

---

## 4.2 재발급 방식

직원이 변경을 요청하거나 노출이 의심되면 관리자가 새 무작위 PIN을 재발급한다.

조건:

```text
6자리 숫자
시스템 내 중복 불가
금지 패턴 사용 불가
이전 PIN 재사용 금지
```

PIN 변경은 관리자 화면의 서버 Mutation으로만 처리한다.

---

# 5. PIN 중복 처리

모든 PIN은 급식길 내부에서 **한 직원에게만 할당**되어야 한다.

다음 상태는 허용하지 않는다.

```text
직원 A
001234

직원 B
001234
```

처리 순서:

```text
무작위 6자리 생성
↓
중복 확인
↓
중복 없음
→ 사용

중복 있음
→ 새 무작위 값 생성
```

PIN 중복 확인은 서버에서 수행한다.

---

# 6. 사용 금지 PIN

개인 지정 PIN에는 명백하게 추측하기 쉬운 패턴을 제한한다.

대표적인 금지값:

```text
000000
111111
222222
123456
654321
121212
```

추가적으로 다음과 같은 지나치게 단순한 패턴도 제한할 수 있다.

- 동일 숫자 반복
- 단순 연속 숫자
- 동일한 짧은 패턴 반복

휴대폰 번호·생년월일·직원 코드 등 알고 있는 정보에서 파생된 PIN은 사용하지 않는다.

---

# 7. PIN 변경

PIN 변경은 다음 상황에서 허용한다.

- 직원의 PIN 변경 요청
- 시스템 점검 중 중복 또는 취약 패턴 발견
- PIN 노출 또는 보안 문제 발생
- 관리자의 PIN 초기화
- 퇴사 후 재입사 등 계정 재설정

PIN 변경 시 기존 로그인 세션을 계속 유지할지 여부는 보안 상황에 따라 서버에서 결정한다.

PIN 유출이 의심되는 경우 기존 세션을 함께 폐기한다.

---

# 8. PIN 저장 원칙

PIN 원문은 Firestore에 저장하지 않는다.

다음과 같은 평문 저장은 금지한다.

```text
pin: "345678"
```

로그, 감사 기록, Client Cache에도 PIN 원문을 남기지 않는다.

PIN 인증에는 서버 전용 Lookup 구조와 검증용 Hash를 사용한다.

---

# 9. 인증 데이터 구조

개념적인 서버 데이터 구조는 다음과 같다.

```text
pinIndexes/{pinLookupKey}
    employeeId

authCredentials/{employeeId}
    pinHash
    pinVersion
    sessionVersion
    failedAttemptCount
    lockedUntil
    updatedAt
```

`pinIndexes`와 `authCredentials`는 일반 Client에서 접근할 수 없다.

Cloud Functions 또는 신뢰된 서버 환경만 접근한다.

---

# 10. PIN 조회

PIN만으로 직원을 식별해야 하므로 서버는 입력된 PIN에서 결정적인 Lookup Key를 생성한다.

개념 흐름:

```text
사용자 PIN
↓
서버 Secret 기반 HMAC
↓
pinLookupKey
↓
employeeId 확인
```

Firestore에는 PIN 원문 대신 Lookup Key만 저장한다.

---

# 11. PIN 검증

직원을 식별한 뒤 서버는 별도의 `pinHash`를 이용해 실제 PIN을 검증한다.

개념:

```text
PIN 입력
↓
Lookup Key로 직원 식별
↓
PIN Hash 검증
↓
인증 성공
```

Lookup 용 값과 인증 검증용 값을 분리한다.

---

# 12. 일반 직원 로그인 처리

로그인 전체 흐름은 다음과 같다.

```text
급식길 실행
↓
PIN 입력
↓
Cloud Function
↓
App Check 확인
↓
로그인 시도 제한 확인
↓
PIN Lookup
↓
직원 확인
↓
직원 active 상태 확인
↓
PIN Hash 검증
↓
직원 권한 확인
↓
Firebase Custom Token 발급
↓
Firebase Authentication 로그인
↓
업무 화면 진입
```

Client가 자신의 직원 ID나 역할을 직접 지정하지 않는다.

---

# 13. 로그인 실패 메시지

PIN 하나만 입력하므로 실패 메시지도 단순하게 유지한다.

권장:

```text
PIN을 확인해주세요.
```

비정상적인 반복 실패:

```text
로그인 시도가 여러 번 실패했습니다.
잠시 후 다시 시도해주세요.
```

서버 내부의 구체적인 실패 원인은 사용자에게 노출하지 않는다.

---

# 14. 로그인 시도 제한

PIN은 6자리 숫자이므로 무차별 입력 공격에 대한 방어가 필수다.

기본 정책:

```text
1~4회 실패
→ 재시도 허용

5회 연속 실패
→ 일정 시간 로그인 제한

반복적인 잠금 발생
→ 관리자 확인 대상
```

정확한 잠금 시간과 Rate Limit 값은 구현 단계에서 조정한다.

로그인 성공 시 연속 실패 횟수는 초기화한다.

---

# 15. 로그인 상태 유지

급식길은 **지속 로그인**을 기본 정책으로 한다.

최초 로그인 성공 후:

```text
앱 종료
↓
로그인 유지

PWA 종료
↓
로그인 유지

브라우저 종료
↓
로그인 유지

기기 재부팅
↓
로그인 유지
```

사용자가 직접 로그아웃하지 않는 한 일반적으로 PIN을 다시 입력하지 않는다.

---

# 16. 자동 로그아웃 정책

일정 시간 앱을 사용하지 않았다는 이유만으로 자동 로그아웃시키지 않는다.

예를 들어 다음과 같은 정책은 사용하지 않는다.

```text
30분 미사용
→ 자동 로그아웃
```

현장 직원은 학교 간 이동 등으로 앱을 간헐적으로 사용하므로 이러한 방식은 업무 흐름을 방해할 가능성이 크다.

---

# 17. 명시적 로그아웃

사용자는 설정 화면에서 직접 로그아웃할 수 있다.

```text
설정

내 정보
앱 정보

[ 로그아웃 ]
```

로그아웃을 선택하면:

- Firebase Authentication 로그아웃
- 현재 사용자 Memory Cache 제거
- 민감한 임시 데이터 제거
- 사진 Blob URL 제거
- 현재 직원 세션정보 제거

를 수행한다.

---

# 18. 관리자 강제 로그아웃

사용자가 로그아웃하지 않았더라도 회사에서 기존 세션을 강제로 종료할 수 있어야 한다.

적용 상황:

- 휴대폰 분실
- 퇴사
- 휴직
- PIN 노출
- 계정 도용 의심
- 역할 변경
- 보안 사고
- 관리자의 수동 세션 종료

---

# 19. sessionVersion

직원마다 세션 버전을 유지한다.

예:

```text
sessionVersion: 7
```

세션을 강제로 무효화해야 하면:

```text
7
→
8
```

로 증가시킨다.

기존 인증 정보는 더 이상 현재 Version과 일치하지 않으므로 접근을 거부한다.

---

# 20. 권한 변경과 세션

직원의 업무 권한이 변경되면 기존 로그인 상태를 그대로 신뢰하지 않는다.

예:

```text
납품
→
홍보·영업
```

또는:

```text
홍보·영업
→
납품
```

변경 흐름:

```text
관리자 권한 변경
↓
sessionVersion 갱신
↓
기존 세션 무효화
↓
재인증
↓
새 권한 적용
```

특히 홍보 권한이 제거됐을 때 기존 Sales Cache와 인증 정보가 남지 않도록 한다.

---

# 21. 직원 비활성화

직원 계정 상태:

```text
active
disabled
```

`disabled` 직원은 신규 로그인할 수 없다.

이미 로그인된 직원이 비활성화되면 기존 세션도 무효화한다.

퇴사 직원을 단순히 앱 메뉴에서 숨기는 방식으로 처리하지 않는다.

---

# 22. 일반 직원 권한 구조

PIN 로그인 방식이 변경돼도 기존 데이터 권한 구조는 유지한다.

## 납품 사용자

접근 가능:

- 학교 기본정보
- 학교 사진
- 학교 현장정보

접근 불가:

- 홍보 담당 구역
- 홍보 방문 기록
- 관심도
- 관심 제품
- 홍보지·샘플
- 커뮤니케이션 참고
- 후속 조치
- 홍보 통계

---

## 홍보·영업 사용자

접근 가능:

- 학교 기본정보
- 학교 사진
- 학교 현장정보
- 홍보·영업정보
- 자신의 구역
- 전체 팀 활동 조회
- 과거 방문 기록

학교 현장정보는 조회·수정할 수 있다.

다른 홍보 직원의 활동은 조회 가능하지만 임의 수정할 수 없다.

---

## 관리자

전체 업무정보와 다음 관리 기능에 접근한다.

- 직원 관리
- PIN 변경
- 직원 비활성화
- 권한 변경
- 세션 폐기
- 월별 구역 배정
- CSV 관리
- 감사 로그

---

# 23. 관리자 인증

관리자는 직원용 PIN 인증과 분리한다.

관리자 로그인은 **Google 계정 인증 + 서버 관리 허용목록 + `admin` 역할 확인**을 모두 통과해야 한다.

Google 인증 성공만으로 관리자 권한을 부여하지 않는다. 서버 허용목록에 없는 계정, 비활성 계정, `admin` 역할이 제거된 계정은 관리자 기능에 접근할 수 없다.

관리자 계정은 일반 직원 PIN만으로 관리자 권한을 얻지 않는다.

---

# 24. 공용 기기

로그인 유지가 기본 정책이므로 공용 기기에서는 특히 주의가 필요하다.

공용 PC나 공동 태블릿 사용 시:

- 사용 종료 후 명시적 로그아웃
- 직원별 Cache 격리
- 로그아웃 시 홍보 관련 Persistent Cache 제거
- 다른 직원 로그인 전에 이전 직원 정보 제거

를 적용한다.

로그인 화면 또는 설정에서 공용 기기 여부를 별도로 받을지는 실제 운영 환경을 확인한 후 결정한다.

---

# 25. 개인 기기

개인 휴대전화에서는 다음 정책을 적용한다.

- 로그인 지속
- 반복 PIN 입력 없음
- 학교 검색 Cache 유지 가능
- 학교 현장정보 Cache 유지 가능
- 권한 범위 내 홍보 Cache 사용 가능

단, 서버에서 세션이 폐기됐으면 재로그인이 필요하다.

---

# 26. App Check

PIN 로그인 API에는 App Check를 적용한다.

목적:

```text
사용자 인증
+
정상 급식길 앱 요청 여부 확인
```

PIN 인증만으로 Client 요청의 신뢰성을 판단하지 않는다.

---

# 27. 로그인 API 보안

로그인 API는 최소 다음 보호장치를 가진다.

```text
App Check

Rate Limit

PIN Lookup 보호

PIN Hash 검증

직원 활성 상태 확인

실패 횟수 관리

잠금 정책

Audit Log
```

---

# 28. 감사 로그

최소 다음 인증 이벤트를 기록한다.

```text
LOGIN_SUCCESS

LOGIN_FAILURE_LOCK

LOGOUT

PIN_CHANGED

PIN_RESET

EMPLOYEE_DISABLED

ROLE_CHANGED

SESSION_REVOKED
```

PIN 원문은 감사 로그에 기록하지 않는다.

---

# 29. 로그인 UX

최초 로그인은 가능한 한 단순하게 유지한다.

```text
급식길

대전 학교 급식실 현장정보


○  ○  ○  ○  ○  ○


[ 시작하기 ]
```

입력된 숫자는 직접 표시하지 않고 점 또는 시각적 Indicator로 표시한다.

디자인 시스템의 Liquid Glass 재질을 PIN 입력 영역과 주요 버튼에 사용할 수 있다.

---

# 30. PIN 입력 인터랙션

숫자를 입력할 때:

```text
○ ○ ○ ○ ○ ○

↓ 입력

● ● ● ○ ○ ○
```

각 Indicator는 짧고 절제된 Animation을 사용할 수 있다.

마지막 6번째 숫자가 입력되면 로그인 버튼을 활성화한다.

자동 로그인 제출 여부는 실제 기기 사용성을 확인한 뒤 결정한다.

---

# 31. 재방문 UX

정상 로그인 상태가 남아 있으면 사용자는 로그인 화면을 보지 않는다.

```text
앱 실행
↓
기존 인증 상태 확인
↓
세션 유효
↓
바로 업무 화면
```

예를 들어 홍보 직원이라면:

```text
급식길 실행
↓
내 구역
```

으로 바로 진입한다.

---

# 32. 세션이 무효화된 경우

관리자가 기존 세션을 폐기한 경우:

```text
인증 정보를 다시 확인해야 합니다.

[ 로그인 ]
```

화면으로 전환한다.

사용자가 이유를 알 필요가 있는 경우:

```text
로그인 정보가 변경되었습니다.
다시 로그인해주세요.
```

정도로 안내한다.

기술적인 Token 오류를 그대로 표시하지 않는다.

---

# 33. 보안상 주요 전제

6자리 PIN은 입력 공간 자체가 작아 완전한 비밀번호보다 추측 가능성이 높다.

따라서 본 설계는 **PIN 자체의 복잡성보다 서버의 접근 통제를 함께 사용하는 구조**를 전제로 한다.

반드시 적용:

- PIN 중복 차단
- 로그인 Rate Limit
- 반복 실패 잠금
- App Check
- Firestore Security Rules
- Storage Security Rules
- 관리자 강제 세션 폐기
- Audit Log

PIN 단독 로그인이라는 UX 단순화를 이유로 서버 보안을 약화해서는 안 된다.

---

# 34. MVP 완료 기준

인증 기능은 다음 조건을 모두 충족해야 완료로 본다.

- 직원코드 입력 없음
- 일반 직원 PIN 단독 로그인
- 6자리 PIN
- 서버 무작위 PIN 발급
- 휴대폰 번호 등 개인·업무정보에서 PIN 파생 금지
- PIN 중복 불가
- PIN 평문 저장 없음
- 로그인 시도 제한
- 반복 실패 잠금
- 최초 로그인 후 로그인 상태 지속
- 앱 재실행 후 자동 로그인
- 자동 시간초과 로그아웃 없음
- 사용자 직접 로그아웃 가능
- 관리자 강제 세션 폐기 가능
- 직원 비활성화 가능
- 역할 변경 후 기존 권한 즉시 무효화
- App Check 적용
- 감사 로그 기록
- 납품 사용자의 홍보정보 접근 차단

---

# 35. 최종 인증 정책

급식길의 일반 직원 인증 원칙은 다음 한 문장으로 정의한다.

> **직원은 기억하기 쉬운 고유 6자리 PIN 하나로 최초 로그인하고, 이후에는 직접 로그아웃하거나 관리자가 세션을 폐기하기 전까지 로그인 상태를 유지한다.**

운영 원칙은 다음과 같다.

```text
간단한 로그인
+
지속 로그인
+
강한 서버 통제
```

즉 급식길은 반복적인 로그인 절차로 보안을 확보하는 것이 아니라, **최초 인증을 간단하게 하고 이후 데이터 접근 권한과 세션 통제를 서버에서 엄격하게 관리하는 방식**을 사용한다.

---

# 36. 문서 상태

본 문서는 **급식길 인증·권한·보안 설계서 v1.3**다.

기존 인증 관련 내용 중 다음 사항은 본 문서가 우선한다.

```text
직원코드 + PIN
→ 폐기

랜덤 8자리 PIN 기본
→ 폐기

개인 PIN 단독 로그인
→ 채택

휴대폰 번호 기반 PIN
→ 폐기

서버 발급 무작위 6자리 PIN
→ 채택

앱 종료 후 로그인 유지
→ 채택

미사용 시간 기준 자동 로그아웃
→ 사용하지 않음
```

데이터 접근 권한, Firestore/Storage Security Rules, 홍보정보 보호, 사진 권한, CSV, 감사 로그 등 기존 보안 설계의 나머지 원칙은 계속 유지한다.
